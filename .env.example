# Copier en `.env`. Le fichier `.env` n'est jamais commite (voir .gitignore).

# Clef API Anthropic. Absente => le pipeline bascule automatiquement sur le
# client LLM simule (src/llm/mock-client.ts) et tourne hors ligne.
ANTHROPIC_API_KEY=

# Modele utilise par tous les agents.
MEDIA_MODEL=claude-opus-5

# `mock` (defaut, hors ligne) ou `live` (vrais appels API + vraies sources).
MEDIA_MODE=mock
